name: novel-audio
channels:
  - conda-forge
dependencies:
  - python=3.11
  - ffmpeg
  - libsndfile
  - pip
# espeak-ng is NOT packaged on conda-forge. `make audio-install` instead pip-installs
# `espeakng-loader`, which bundles the espeak-ng shared library cross-platform.
